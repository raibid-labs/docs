{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "version": "1.0.0",
  "description": "Template ignorelist configuration for docs-internal repository",
  "repositories": [
    "docs",
    "docs-internal"
  ],
  "patterns": [
    "fork-*",
    "archive-*",
    "deprecated-*"
  ],
  "exclude_forks": true,
  "exclude_archived": true,
  "exclude_private": false,
  "require_docs_directory": true,
  "notes": [
    "This is the configuration for the internal docs repository.",
    "Key difference: exclude_private is set to false to include private repos.",
    "Copy this to docs-internal/config/ignorelist.json when setting up."
  ]
}
